# Jeson联机渲染发送口（便携双击版）

你可以把整套目录当“便携版”使用，**双击 `.cmd` 文件启动**，不需要手敲长命令。

## 双击启动入口（Windows）

- `启动渲染节点托盘.cmd`：渲染主机双击启动（右下角托盘）
- `启动管理端服务.cmd`：管理端/服务器双击启动 FastAPI 服务
- `打开任务看板.cmd`：双击直接打开网页看板

## 你报错的原因（PowerShell）

你截图里用了 `cd /d ...`，这是 **cmd.exe 语法**，不是 PowerShell 语法。

- 在 **PowerShell** 里应该用：
  ```powershell
  Set-Location "D:\Blender\addons\Jeson联机渲染"
  ```
- 或直接双击上面的 `.cmd` 启动文件（推荐）。

---

## 当前联机渲染配置

- 缓存目录：`\\wj114\01 完全共享\联机渲染\渲染缓存`
- 临时渲染输出：`\\wj114\01 完全共享\联机渲染\渲染输出`
- 若发送时填写“指定输出路径”，渲染完成后会复制一份到指定路径
- 面板按钮：`发送渲染队列` / `发送当前文件` / `查看任务列表`
- 任务列表网页：`http://192.168.30.212:8000/dashboard`
- 渲染主机固定：`WK1021` `WK1042` `WK1043` `WJ410`

---

## 第一次安装（仅一次）

```bash
pip install -r requirements.txt
```

---

## 日常使用（新手推荐）

### 1) 管理端机器

双击：`启动管理端服务.cmd`

### 2) 每台渲染机

双击：`启动渲染节点托盘.cmd`

托盘功能：
- 自动最小化到右下角
- 绿点=运行中，红点=停止
- 右键菜单：启动/停止、开机自启、打开配置、退出

配置文件路径：`%USERPROFILE%/.jeson_worker_config.json`

> 第一次运行建议先在托盘菜单点“打开配置文件”，把 `host` 改为对应主机名（WK1021/WK1042/WK1043/WJ410），保存后重启托盘程序。

### 3) 查看状态

双击：`打开任务看板.cmd`

---

## Blender 插件安装（只放插件目录）

`Jeson联机渲染/__init__.py` 必须在插件目录根。

```text
Jeson联机渲染/
  __init__.py
```

不要把整个项目根目录（含 `agent/`、`server/`）当 Blender 插件目录。

---

## Blender 发送任务

在“输出 > 联机发送”里：
- `发送渲染队列`：把 `scene.render_queue` 中任务拷贝到缓存目录后提交。
- `发送当前文件`：把当前 `.blend` 拷贝到缓存目录，命名为 `文件名_五位随机数.blend` 后提交。
- `查看任务列表`：直接打开网页看板。
